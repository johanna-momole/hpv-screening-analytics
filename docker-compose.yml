version: "3.9"

services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data       # persist generated database outside container
      - ./screenshots:/app/screenshots
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
